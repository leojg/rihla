# Rihla credentials - copy to .env and fill in. .env is gitignored.
#
# Amadeus Self-Service API (https://developers.amadeus.com).
# Create a free app to get a key/secret (free tier ~2,000 flight searches/month).
# Leave these blank to run offline against the built-in MockFetcher.
AMADEUS_CLIENT_ID=
AMADEUS_CLIENT_SECRET=
